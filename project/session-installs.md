# Session-installed tooling (remove when done working on this project)

- `go` (via Homebrew, `brew install go`) — needed to build/test `tools/tf-hcl-graph`.
  Not present on this machine before this project started. Remove with:
  `brew uninstall go`
